{
  "private": true,
  "scripts": {
    "build": "tsc -b packages/htlc packages/redshift.js",
    "test": "lerna run test --stream --parallel",
    "clean": "rimraf packages/**/dist"
  },
  "devDependencies": {
    "@ts-tools/node": "^0.7.4",
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.12",
    "chai": "^4.2.0",
    "lerna": "^2.9.0",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.2.1"
  },
  "workspaces": [
    "packages/**"
  ]
}
